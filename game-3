<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Random Name Picker Game</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to right, #8e44ad, #3498db);
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    h1 {
      font-size: 3rem;
      margin-bottom: 20px;
    }

    #name-display {
      font-size: 4rem;
      margin: 30px 0;
      padding: 20px 40px;
      border: 3px solid white;
      border-radius: 20px;
      background-color: rgba(255, 255, 255, 0.1);
      min-width: 300px;
      text-align: center;
    }

    button {
      font-size: 1.5rem;
      padding: 15px 30px;
      border: none;
      background-color: #f1c40f;
      color: #2c3e50;
      border-radius: 10px;
      cursor: pointer;
      transition: 0.3s ease;
    }

    button:hover {
      background-color: #d4ac0d;
    }
  </style>
</head>
<body>

  <h1>🎲 Random Name Picker 🎲</h1>
  <div id="name-display">Siapa yang terpilih?</div>
  <button onclick="pickRandomName()">Pilih Nama</button>

  <script>
    const names = ["Salsa", "Iren", "Bima", "Bintang", "Bulan", "Satria", "Icul", "Amel"];
    const display = document.getElementById("name-display");

    function pickRandomName() {
      let count = 0;
      const interval = setInterval(() => {
        const randomIndex = Math.floor(Math.random() * names.length);
        display.textContent = names[randomIndex];
        count++;
        if (count > 20) {  // berhenti setelah 20 pergantian nama (efek animasi)
          clearInterval(interval);
        }
      }, 100); // kecepatan animasi (semakin kecil semakin cepat)
    }
  </script>

</body>
</html>
